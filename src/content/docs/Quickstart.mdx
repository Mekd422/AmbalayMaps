import CodeBlock from "../../Components/CodeBlock.tsx";

export default function Quickstart() {

  const bashCode = `curl -X GET "https://api.ambalaymaps.com/v1/geocode?address=Addis+Ababa&key=YOUR_API_KEY"`;

  const jsonCode = `{
  "status": "success",
  "results": [
    {
      "address": "Addis Ababa, Ethiopia",
      "latitude": 9.03,
      "longitude": 38.74
    }
  ]
}`;

  const mapUrl = `https://api.ambalaymaps.com/v1/staticmap?center=9.03,38.74&zoom=12&size=600x400&key=YOUR_API_KEY`;

  return (
    <div className="bg-black text-gray-300 min-h-screen p-8 font-sans">

      {/* Header */}
      <header className="mb-10">
        <h1 className="text-white text-5xl font-bold mb-4 tracking-tight">
          Quickstart Guide
        </h1>
        <div className="h-[1px] bg-gray-800 w-full mb-6"></div>
        <p className="text-gray-400 text-lg">
          Follow this quick guide to start using the AmbaLay Maps API Immediately:
        </p>
      </header>

      {/* Step 1 */}
      <section className="mb-12">
        <h3 className="text-[#4d79ff] text-xl font-bold mb-3">
          1. Sign Up and Get an API Key
        </h3>
        <p className="text-gray-400 leading-relaxed max-w-4xl">
          Create a free account on the AmbaLay Maps dashboard and obtain your unique API key from the Developer Portal. This key authenticates your requests.
        </p>
      </section>

      {/* Step 2 */}
      <section className="mb-12">
        <h3 className="text-[#4d79ff] text-xl font-bold mb-3">
          2. Make a Simple Request
        </h3>

        <p className="text-gray-400 mb-6 max-w-4xl">
          Test your setup by making a basic API call. For example, you can try a geocoding request to convert a city name to coordinates. Using the command line with curl:
        </p>

        <CodeBlock code={bashCode} language="bash" />

        <p className="text-gray-400 mb-6">
          This will return a JSON response with the latitude and longitude for Addis Ababa. A successful response looks like:
        </p>

        <CodeBlock code={jsonCode} language="json" />
      </section>

      {/* Step 3 */}
      <section className="mb-12">
        <h3 className="text-[#4d79ff] text-xl font-bold mb-3">
          3. Display a Map (Optional)
        </h3>

        <p className="text-gray-400 mb-6 max-w-4xl">
          As a quick visualization test, you can retrieve a static map. For example, open the following URL in a browser (with your API key) to get a static map centered on Addis Ababa:
        </p>

        <CodeBlock code={mapUrl} language="bash" />

        <p className="text-gray-400 leading-relaxed italic border-l-2 border-[#4d79ff] pl-4">
          This will prompt a PNG image download of the specified map. You can embed this in an HTML page to quickly display a map (see 
          <span className="text-[#8cff2e] mx-1 cursor-pointer hover:underline">
            Embedding a Map
          </span> 
          in the Tutorials section).
        </p>
      </section>

      {/* Step 4 */}
      <footer className="mt-20 pt-10 ">
        <h3 className="text-[#4d79ff] font-bold text-xl mb-4">
          4. Explore Further
        </h3>

        <p className="text-gray-400 leading-relaxed">
          With your environment set up and a successful test, you can start integrating more API features. Continue to the 
          <span className="text-[#8cff2e] mx-1 cursor-pointer hover:underline font-medium">
            Getting Started
          </span> 
          section for a detailed setup guide, or jump to the 
          <span className="text-[#8cff2e] ml-1 cursor-pointer hover:underline font-medium">
            API Reference
          </span> 
          for specific endpoints and capabilities.
        </p>
      </footer>

    </div>
  );
}