import CodeBlock from "../../Components/CodeBlock.tsx";

export default function RouteCalculation() {
  const curlExample = `curl -H "x-api-key: YOUR_API_KEY" "https://api.ambalaymaps.com/v1/route?origin=9.03,38.74&destination=9.00,38.76&mode=driving"`;

  const jsonResponse = `{
  "status": 200,
  "message": "ROUTE_CALCULATION_SUCCESS",
  "data": {
    "status": "success",
    "route": {
      "distance": "6.5 km",
      "duration": "00:08",
      "steps": [
        {
          "type": 1,
          "instruction": "Drive northwest.",
          "travel_mode": "drive",
          "travel_type": "car"
        },
        {
          "type": 10,
          "instruction": "Turn right onto Kasanchis Interluxury Street.",
          "street_names": [
            "Kasanchis Interluxury"
          ],
          "travel_mode": "drive",
          "travel_type": "car"
        }
      ]
    }
  }
}`;

  return (
    <div className="bg-black text-gray-300 min-h-screen p-8 font-sans">
      {/* Header Section */}
      <header className="mb-10">
        <h1 className="text-white text-5xl font-bold mb-4 tracking-tight">
          Route Calculation
        </h1>
        <div className="h-[1px] bg-gray-800 w-full mb-6"></div>
        <p className="text-gray-400 text-lg leading-relaxed">
          <strong className="text-white">Purpose:</strong> Calculates a route
          (driving directions) from an origin point to a destination point. The
          response includes the total distance, estimated duration, and
          step-by-step turn instructions for the route.
        </p>
      </header>

      {/* Endpoint Information */}
      <section className="mb-12">
        <h3 className="text-[#4d79ff] text-xl font-bold mb-4">Endpoint</h3>
        <div className="space-y-4">
          <p className="font-mono bg-[#1a1a1a] px-3 py-1 inline-block rounded text-white">
            GET /route
          </p>
          <p className="text-gray-400">
            <strong className="text-white">Full URL:</strong>{" "}
            <span className="text-[#00d1ff]">
              https://api.ambalaymaps.com/v1/route
            </span>
          </p>
        </div>
      </section>

      {/* Parameters Table */}
      <section className="mb-12">
        <h3 className="text-[#4d79ff] text-xl font-bold mb-6">
          Required Query Parameters
        </h3>
        <div className="overflow-x-auto rounded-xl border border-gray-800">
          <table className="w-full text-left border-collapse">
            <thead className="bg-[#1a1a1a] text-gray-400 text-sm uppercase tracking-wider">
              <tr>
                <th className="px-6 py-4 font-semibold border-b border-gray-800">
                  Parameter
                </th>
                <th className="px-6 py-4 font-semibold border-b border-gray-800">
                  Type
                </th>
                <th className="px-6 py-4 font-semibold border-b border-gray-800">
                  Required
                </th>
                <th className="px-6 py-4 font-semibold border-b border-gray-800">
                  Description
                </th>
              </tr>
            </thead>
            <tbody className="divide-y divide-gray-800">
              <tr>
                <td className="px-6 py-4 font-mono text-[#8cff2e]">origin</td>
                <td className="px-6 py-4">string</td>
                <td className="px-6 py-4">Yes</td>
                <td className="px-6 py-4 text-sm">
                  The starting point as "lat,lon". Example:{" "}
                  <code className="text-white">origin=9.03,38.74</code>
                </td>
              </tr>
              <tr>
                <td className="px-6 py-4 font-mono text-[#8cff2e]">
                  destination
                </td>
                <td className="px-6 py-4">string</td>
                <td className="px-6 py-4">Yes</td>
                <td className="px-6 py-4 text-sm">
                  The destination point as "lat,lon". Example:{" "}
                  <code className="text-white">destination=9.00,38.76</code>
                </td>
              </tr>
              <tr>
                <td className="px-6 py-4 font-mono text-[#8cff2e]">mode</td>
                <td className="px-6 py-4">string</td>
                <td className="px-6 py-4">No</td>
                <td className="px-6 py-4 text-sm">
                  Travel mode. Supports <code className="text-white">driving</code>. Defaults to driving.
                </td>
              </tr>
            </tbody>
          </table>
        </div>
        <p className="mt-4 text-sm text-gray-400 italic">
          <strong className="text-white">Headers:</strong>{" "}
          <span className="text-[#8cff2e]">x-api-key: Your API key</span>
        </p>
      </section>

      {/* Example Request */}
      <section className="mb-12">
        <h3 className="text-[#4d79ff] text-xl font-bold mb-3">Example Request</h3>
        <p className="text-gray-400 mb-6">
          This request asks for a driving route from the coordinate 9.03,38.74 to 9.00,38.76 (latitude, longitude).
        </p>
        
        <CodeBlock code={curlExample} language="bash" />
        
        <p className="text-gray-500 text-sm italic mt-2">
          Note: Ensure you include your API key in the request header.
        </p>
      </section>

      {/* Example Response */}
      <section className="mb-12">
        <h3 className="text-[#4d79ff] text-xl font-bold mb-3">Example Response</h3>
        <CodeBlock code={jsonResponse} language="json" />

        <div className="mt-8 space-y-6">
          <h4 className="text-white text-lg font-medium">In this JSON response:</h4>
          <ul className="space-y-4 list-none">
            <li className="text-gray-400 leading-relaxed">
              <span className="text-[#8cff2e] font-mono">"distance"</span> is the total route length (here 6.5 km).
            </li>
            <li className="text-gray-400 leading-relaxed">
              <span className="text-[#8cff2e] font-mono">"duration"</span> is the estimated travel time (here 00:08, i.e., 8 minutes).
            </li>
            <li className="text-gray-400 leading-relaxed">
              <span className="text-[#8cff2e] font-mono">"steps"</span> is an array of turn-by-turn instructions. Each step includes a maneuver <span className="text-white">"instruction"</span> (e.g., "Drive northwest.", "Turn right onto ... Street") and other details. The type is a numeric code for the maneuver type, and travel_mode/travel_type indicate the mode of travel. (In driving mode, travel_mode is "drive" with travel_type "car".)
            </li>
          </ul>
        </div>
      </section>

      {/* Status Codes Section */}
      <footer className="mt-20 pt-10 border-t border-gray-900 pb-20">
        <h3 className="text-white font-bold text-xl mb-6">
          Status Codes and Error Messages:
        </h3>
        <div className="space-y-6 text-sm">
          <p className="leading-relaxed">
            <strong className="text-white">200 OK:</strong> Successfully found a route. The JSON message will be <span className="text-[#8cff2e]">"ROUTE_CALCULATION_SUCCESS"</span> and <span className="text-[#8cff2e]">data.route</span> will contain the route details.
          </p>
          <p className="leading-relaxed">
            <strong className="text-white">401 Unauthorized:</strong> Missing or invalid API key. The response will have <span className="text-[#8cff2e]">status: 401</span> and <span className="text-[#8cff2e]">message: "API_KEY_REQUIRED"</span>. Ensure you provided the x-api-key.
          </p>
          <p className="leading-relaxed">
            <strong className="text-white">500 Internal Server Error:</strong> The route could not be calculated. This can happen if required parameters are missing or if no route is found. The message may be <span className="text-[#8cff2e]">"ROUTE_CALCULATION_ERROR"</span> or a similar error indicator. For example, if you omit the origin or destination parameter, you will receive an error. Double-check that both origin and destination are provided and properly formatted.
          </p>
        </div>
      </footer>
    </div>
  );
}