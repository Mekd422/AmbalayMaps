import CodeBlock from "../../Components/CodeBlock.tsx";

export default function OptimizedRouteAPI() {
  const curlExample = `curl -G 'https://api.ambalaymaps.com/v1/matrix' \\
  -H "x-api-key: YOUR_API_KEY" \\
  --data-urlencode 'origins=40.73061,-73.935242|40.712776,-74.005974' \\
  --data-urlencode 'destinations=40.758896,-73.98513|40.706192,-74.00916' \\
  --data-urlencode 'mode=driving'`;

  const jsonResponse = `{
  "status": 200,
  "message": "ROUTE_OPTIMIZED",
  "data": {
    "trip": {
      "legs": [
        {
          "maneuvers": [
            {
              "instruction": "Drive northwest on B11/Awash - Asseb highway.",
              "time": 36.866,
              "length": 0.45,
              "begin_shape_index": 0,
              "end_shape_index": 14
            }
          ],
          "summary": {
            "time": 1734.185,
            "length": 18.95
          },
          "shape": "encoded_polyline_string"
        }
      ]
    },
    "summary": {
      "time": 4731.821,
      "length": 41.676
    },
    "units": "kilometers"
  }
}`;

  return (
    <div className="bg-black text-gray-300 min-h-screen p-8 font-sans">
      {/* Header Section */}
      <header className="mb-10">
        <h1 className="text-white text-5xl font-bold mb-4 tracking-tight !mt-0">
          Optimized Route API
        </h1>
        <div className="h-[1px] bg-gray-800 w-full mb-6"></div>
        <p className="text-gray-400 text-lg leading-relaxed">
          <strong className="text-white">Purpose:</strong> Calculates the most efficient sequence and path to visit multiple locations.
        </p>
      </header>

      {/* Base URL & Auth */}
      <section className="mb-12">
        <h3 className="text-[#4d79ff] text-xl font-bold mb-4 !mt-0">Base URL & Authentication</h3>
        <div className="space-y-4 mb-8">
          <p className="text-gray-400">
            <strong className="text-white">Base URL:</strong> <span className="text-[#00d1ff]">https://api.ambalaymaps.com/v1</span>
          </p>
          <p className="text-gray-400">
            <strong className="text-white">Auth:</strong> <code className="text-[#8cff2e]">X-API-Key: YOUR_API_KEY</code>
          </p>
        </div>

        <h3 className="text-[#4d79ff] text-xl font-bold mb-4">Modes & Units</h3>
        <ul className="space-y-2 text-gray-400 mb-6">
          <li><strong className="text-white">Modes:</strong> driving, bicycle, pedestrian.</li>
          <li><strong className="text-white">Units:</strong> Distances are in kilometers. Durations are returned in seconds.</li>
        </ul>
      </section>

      {/* Query Parameters Table */}
      <section className="mb-12">
        <h3 className="text-[#4d79ff] text-xl font-bold mb-6">Required Query Parameters</h3>
        <div className="overflow-x-auto rounded-xl border border-gray-800 shadow-2xl">
          <table className="w-full text-left border-collapse">
            <thead className="bg-[#1a1a1a] text-gray-400 text-sm uppercase tracking-wider">
              <tr>
                <th className="px-6 py-4 font-semibold border-b border-gray-800">Parameter</th>
                <th className="px-6 py-4 font-semibold border-b border-gray-800">Type</th>
                <th className="px-6 py-4 font-semibold border-b border-gray-800">Required</th>
                <th className="px-6 py-4 border-b border-gray-800">Description</th>
              </tr>
            </thead>
            <tbody className="divide-y divide-gray-800">
              <tr>
                <td className="px-6 py-4 font-mono text-[#8cff2e]">origins</td>
                <td className="px-6 py-4">string</td>
                <td className="px-6 py-4">Yes</td>
                <td className="px-6 py-4 text-sm text-gray-400">Coordinates as <code className="text-white">lat,lon|lat,lon...</code></td>
              </tr>
              <tr>
                <td className="px-6 py-4 font-mono text-[#8cff2e]">destinations</td>
                <td className="px-6 py-4">string</td>
                <td className="px-6 py-4">Yes</td>
                <td className="px-6 py-4 text-sm text-gray-400">Coordinates as <code className="text-white">lat,lon|lat,lon...</code></td>
              </tr>
              <tr>
                <td className="px-6 py-4 font-mono text-[#8cff2e]">mode</td>
                <td className="px-6 py-4">string</td>
                <td className="px-6 py-4">Optional</td>
                <td className="px-6 py-4 text-sm text-gray-400">driving | bicycle | pedestrian (default: driving)</td>
              </tr>
            </tbody>
          </table>
        </div>
      </section>

      {/* Example Request */}
      <section className="mb-12">
        <h3 className="text-[#4d79ff] text-xl font-bold mb-4">Example Request</h3>
        <CodeBlock code={curlExample} language="bash" />
      </section>

      {/* Example Response */}
      <section className="mb-12">
        <h3 className="text-[#4d79ff] text-xl font-bold mb-4">Example Response</h3>
        <CodeBlock code={jsonResponse} language="json" />
      </section>

      {/* JSON Field Explanation */}
      <section className="mb-12">
        <h4 className="text-white font-bold mb-4 text-lg">Response Field Details:</h4>
        <ul className="space-y-4 text-gray-400 leading-relaxed">
          <li><code className="text-[#8cff2e]">trip.legs[].maneuvers[]</code> — Step-by-step instructions with time (sec) and length (km).</li>
          <li><code className="text-[#8cff2e]">trip.legs[].summary</code> — Per-leg summary including time and length.</li>
          <li><code className="text-[#8cff2e]">summary</code> — Overall trip summary for the entire optimized route.</li>
          <li><code className="text-[#8cff2e]">units</code> — Specifies the measurement system (kilometers).</li>
        </ul>
      </section>

      {/* Status Codes */}
      <footer className="mt-20 pt-10 border-t border-gray-900 pb-20">
        <h3 className="text-[#4d79ff] font-bold text-xl mb-6">Common Errors</h3>
        <div className="space-y-6 text-sm text-gray-400">
          <p>
            <strong className="text-white">400 INVALID_QUERY_PARAMETERS:</strong> Occurs if coordinates are improperly formatted or missing.
          </p>
          <p>
            <strong className="text-white">500 MATRIX_CALCULATION_FAILED:</strong> Internal server error during optimization; retry with backoff.
          </p>
          <div className="h-[1px] bg-gray-800 w-full my-6"></div>
          <p className="text-center tracking-wide uppercase text-xs">
            AmbaLay Maps Optimized Route API · Reference Documentation
          </p>
        </div>
      </footer>
    </div>
  );
}