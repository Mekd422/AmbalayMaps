import CodeBlock from "../../Components/CodeBlock.tsx";

export default function StaticMaps() {
  const curlExample = `curl -X GET "https://api.ambalaymaps.com/v1/staticmap?center=9.03,38.74&zoom=12&size=600x400&markers=color:red|9.03,38.74&key=YOUR_API_KEY" -o map.png`;

  const styledMapExample = `curl -X GET "https://api.ambalaymaps.com/v1/staticmap?center=9.03,38.74&zoom=12&size=600x400&maptype=roadmap&style=feature:water|element:geometry|color:0x0099dd&style=feature:road|element:geometry|color:0xeeeeee&key=YOUR_API_KEY" -o styled_map.png`;

  const satelliteMapExample = `curl -X GET "https://api.ambalaymaps.com/v1/staticmap?center=9.03,38.74&zoom=12&size=600x400&maptype=hybrid&key=YOUR_API_KEY" -o satellite_map.png`;

  const htmlUsage = `<img src="https://api.ambalaymaps.com/v1/staticmap?center=9.03,38.74&zoom=12&size=600x400&key=YOUR_API_KEY" alt="Map of Addis Ababa">

<img src="https://api.ambalaymaps.com/v1/staticmap?center=9.03,38.74&zoom=12&size=600x400&markers=color:red|9.03,38.74&key=YOUR_API_KEY" alt="Map with marker">

<img src="https://api.ambalaymaps.com/v1/staticmap?size=600x400&path=color:0x0000ff|weight:5|9.03,38.74|9.04,38.75|9.05,38.76&key=YOUR_API_KEY" alt="Route map in Addis Ababa">`;

  const jsExample = `// Function to generate a static map URL
function getStaticMapUrl(options) {
  const apiKey = 'YOUR_API_KEY';
  const baseUrl = 'https://api.ambalaymaps.com/v1/staticmap';

  // Default options
  const defaultOptions = {
    center: '9.03,38.74',
    zoom: 12,
    size: '600x400',
    maptype: 'roadmap'
  };

  // Merge options
  const mergedOptions = { ...defaultOptions, ...options, key: apiKey };

  // Build query string
  const queryParams = Object.entries(mergedOptions)
    .map(([key, value]) => \`\${key}=\${encodeURIComponent(value)}\`)
    .join('&');

  return \`\${baseUrl}?\${queryParams}\`;
}

// Example: Generate a map with a marker
const mapWithMarker = getStaticMapUrl({
  center: '9.03,38.74',
  zoom: 14,
  size: '800x600',
  markers: 'color:red|9.03,38.74'
});

// Example: Generate a map showing a route
const routeMap = getStaticMapUrl({
  size: '800x600',
  path: 'color:0x0000ff|weight:5|9.03,38.74|9.04,38.75|9.05,38.76'
});

// Use in an image element
document.getElementById('map-container').innerHTML = \`
  <img src="\${mapWithMarker}" alt="Map with marker">
\`;`;

  const pythonExample = `import requests
from PIL import Image
from io import BytesIO

def get_static_map(options, save_path=None):
    """
    Fetch a static map from AmbaLay Maps API
    
    Args:
        options: Dictionary of query parameters
        save_path: Optional file path to save the image
    Returns:
        PIL Image object
    """
    api_key = 'YOUR_API_KEY'
    base_url = 'https://api.ambalaymaps.com/v1/staticmap'
    
    # Default options
    default_options = {
        'center': '9.03,38.74',
        'zoom': 12,
        'size': '600x400',
        'maptype': 'roadmap'
    }
    
    # Merge options
    merged_options = {**default_options, **options, 'key': api_key}
    
    # Get the image
    response = requests.get(base_url, params=merged_options)

    if response.status_code == 200:
        image = Image.open(BytesIO(response.content))
        
        # Save to file if requested
        if save_path:
            image.save(save_path)
            
        return image
    else:
        print(f"Error: {response.status_code}")
        return None

# Example: Get a map with a marker
map_with_marker = get_static_map({
    'center': '9.03,38.74',
    'zoom': 14,
    'markers': 'color:red|9.03,38.74'
}, 'map_with_marker.png')

# Example: Get a map showing a route
route_map = get_static_map({
    'path': 'color:0x0000ff|weight:5|9.03,38.74|9.04,38.75|9.05,38.76'
}, 'route_map.png')

print("Maps saved successfully!")`;

  return (
    <div className="bg-black text-gray-300 min-h-screen p-8 font-sans">
      {/* Header Section */}
      <header className="mb-10">
        <h1 className="text-white text-5xl font-bold mb-4 tracking-tight">
          Static Maps API
        </h1>
        <div className="h-[1px] bg-gray-800 w-full mb-6"></div>
        <p className="text-gray-400 text-lg leading-relaxed">
          Generate static map images with custom markers, paths, and styling for
          embedding in websites or applications without using interactive maps.
        </p>
      </header>

      {/* Endpoint Section */}
      <section className="mb-12">
        <h3 className="text-[#4d79ff] text-xl font-bold mb-4">Endpoint</h3>
        <div className="bg-[#1a1a1a] px-4 py-2 inline-block rounded mb-6">
          <code className="text-[#8cff2e]">GET /staticmap</code>
        </div>

        <h3 className="text-[#4d79ff] text-xl font-bold mb-4">Description</h3>
        <p className="text-gray-400 leading-relaxed mb-4">
          The Static Maps API returns a map image in response to an HTTP
          request. You can specify the location, zoom level, size, and add
          markers or paths. This is useful for displaying maps in contexts where
          JavaScript isn't available or for generating thumbnails.
        </p>
      </section>

      {/* Parameters Table Updated from Source */}
      <section className="mb-12">
        <h3 className="text-[#4d79ff] text-xl font-bold mb-6">
          Query Parameters
        </h3>
        <div className="overflow-x-auto rounded-xl border border-gray-800">
          <table className="w-full text-left border-collapse">
            <thead className="bg-[#1a1a1a] text-gray-400 text-sm uppercase tracking-wider">
              <tr>
                <th className="px-6 py-4 font-semibold border-b border-gray-800">Parameter</th>
                <th className="px-6 py-4 font-semibold border-b border-gray-800">Type</th>
                <th className="px-6 py-4 font-semibold border-b border-gray-800">Required</th>
                <th className="px-6 py-4 font-semibold border-b border-gray-800">Description</th>
              </tr>
            </thead>
            <tbody className="divide-y divide-gray-800">
              <tr>
                <td className="px-6 py-4 font-mono text-[#8cff2e]">center</td>
                <td className="px-6 py-4">string</td>
                <td className="px-6 py-4">Yes¹</td>
                <td className="px-6 py-4 text-sm text-gray-400">The center of the map, specified as "latitude,longitude" (e.g., "9.03,38.74") or a place name.</td>
              </tr>
              <tr>
                <td className="px-6 py-4 font-mono text-[#8cff2e]">zoom</td>
                <td className="px-6 py-4">number</td>
                <td className="px-6 py-4">Yes¹</td>
                <td className="px-6 py-4 text-sm text-gray-400">The zoom level of the map (1-20). Higher values zoom in more.</td>
              </tr>
              <tr>
                <td className="px-6 py-4 font-mono text-[#8cff2e]">size</td>
                <td className="px-6 py-4">string</td>
                <td className="px-6 py-4">Yes</td>
                <td className="px-6 py-4 text-sm text-gray-400">The rectangular dimensions of the map image in pixels, specified as "widthxheight" (e.g., "600x400"). Maximum size is 2048x2048.</td>
              </tr>
              <tr>
                <td className="px-6 py-4 font-mono text-[#8cff2e]">scale</td>
                <td className="px-6 py-4">number</td>
                <td className="px-6 py-4">No</td>
                <td className="px-6 py-4 text-sm text-gray-400">Scale factor for high-resolution displays. Values are 1 (default) or 2.</td>
              </tr>
              <tr>
                <td className="px-6 py-4 font-mono text-[#8cff2e]">format</td>
                <td className="px-6 py-4">string</td>
                <td className="px-6 py-4">No</td>
                <td className="px-6 py-4 text-sm text-gray-400">The format of the resulting image. Options include "png" (default), "png8", "jpg", "jpg-baseline", or "webp".</td>
              </tr>
              <tr>
                <td className="px-6 py-4 font-mono text-[#8cff2e]">maptype</td>
                <td className="px-6 py-4">string</td>
                <td className="px-6 py-4">No</td>
                <td className="px-6 py-4 text-sm text-gray-400">The map type. Options include "roadmap" (default), "satellite", "terrain", or "hybrid".</td>
              </tr>
              <tr>
                <td className="px-6 py-4 font-mono text-[#8cff2e]">markers</td>
                <td className="px-6 py-4">string</td>
                <td className="px-6 py-4">No</td>
                <td className="px-6 py-4 text-sm text-gray-400">Markers to add to the map. Format: "color:red|label:A|9.03,38.74|9.05,38.76" or multiple markers parameters.</td>
              </tr>
              <tr>
                <td className="px-6 py-4 font-mono text-[#8cff2e]">path</td>
                <td className="px-6 py-4">string</td>
                <td className="px-6 py-4">No</td>
                <td className="px-6 py-4 text-sm text-gray-400">Path to draw on the map. Format: "color:0x0000ff|weight:5|9.03,38.74|9.04,38.75|9.05,38.76".</td>
              </tr>
              <tr>
                <td className="px-6 py-4 font-mono text-[#8cff2e]">key</td>
                <td className="px-6 py-4">string</td>
                <td className="px-6 py-4">Yes</td>
                <td className="px-6 py-4 text-sm text-gray-400">Your API key.</td>
              </tr>
            </tbody>
          </table>
        </div>
        <p className="mt-4 text-xs text-gray-500 italic">
          ¹ Either <code className="text-white">center</code> and{" "}
          <code className="text-white">zoom</code> must be specified, or the map
          must be fully specified using <code className="text-white">markers</code>{" "}
          or <code className="text-white">paths</code>.
        </p>
      </section>

      {/* Example Request Section */}
      <section className="mb-12">
        <h3 className="text-[#4d79ff] text-xl font-bold mb-4">Example Request</h3>
        <CodeBlock code={curlExample} language="bash" />
        <p className="text-sm text-gray-500 mt-2">
          This request will save a static map centered on Addis Ababa with a red
          marker to a file named map.png.
        </p>
      </section>

      {/* HTML Usage Section */}
      <section className="mb-12">
        <h3 className="text-[#4d79ff] text-xl font-bold mb-4">
          Using Static Maps in HTML
        </h3>
        <CodeBlock code={htmlUsage} language="html" />
      </section>

      {/* Advanced Examples Section */}
      <section className="mb-12">
        <h3 className="text-[#4d79ff] text-2xl font-bold mb-6">
          Advanced Examples
        </h3>
        <div className="space-y-8">
          <div>
            <h4 className="text-white font-bold mb-3">Custom Map Styling</h4>
            <p className="text-sm text-gray-400 mb-4">
              You can customize the appearance of your static maps by adding style parameters:
            </p>
            <CodeBlock code={styledMapExample} language="bash" />
          </div>

          <div>
            <h4 className="text-white font-bold mb-3">Satellite View</h4>
            <CodeBlock code={satelliteMapExample} language="bash" />
          </div>
        </div>
      </section>

      {/* Code Examples Section */}
      <section className="mb-12">
        <h3 className="text-[#4d79ff] text-2xl font-bold mb-8">Code Examples</h3>

        <div className="mb-12">
          <h4 className="text-white font-bold mb-4 text-lg">JavaScript</h4>
          <CodeBlock code={jsExample} language="javascript" />
        </div>

        <div className="mb-12">
          <h4 className="text-white font-bold mb-4 text-lg">Python</h4>
          <CodeBlock code={pythonExample} language="python" />
        </div>
      </section>

      
    </div>
  );
}